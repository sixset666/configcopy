Функция ПолучитьID(ДокументСсылка) Экспорт
	ОтборДок = Отбор.Найти("Документ");
	Если ОтборДок = Неопределено Тогда
		ОтборДок = Отбор.Добавить("Документ","Документ","Документ");
	КонецЕсли;
	
	ОтборДок.ВидСравнения  = ВидСравнения.Равно;
	ОтборДок.Значение      = ДокументСсылка;
	ОтборДок.Использование = Истина;
	
	Прочитать();
	Если Количество() > 0 Тогда
		Возврат ЭтотОбъект[0].IDКарточкиНаСервере;
	Иначе
		Возврат "";
	КонецЕсли;
КонецФункции