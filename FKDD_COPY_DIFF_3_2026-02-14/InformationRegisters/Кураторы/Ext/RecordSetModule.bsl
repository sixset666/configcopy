
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если ОбЗначениеНеЗаполнено(Запись.Контрагент) Тогда
			Отказ = Истина;
			СтрокаОтказа = "Не заполнено поле ""Контрагент"". Запись невозможна!";
			Продолжить;
		КонецЕсли; 
		Если Запись.Контрагент.ЭтоГруппа Тогда
			Отказ = Истина;
			СтрокаОтказа = "Нельзя привязывать куратора к группе контрагентов.";
			Прервать;
		КонецЕсли;
		Если ОбЗначениеНеЗаполнено(Запись.Куратор) Тогда
			Отказ = Истина;
			СтрокаОтказа = "Не заполнено поле ""Куратор"". Запись невозможна!";
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если Отказ Тогда
		Сообщить(СтрокаОтказа);
	КонецЕсли; 
	
КонецПроцедуры
