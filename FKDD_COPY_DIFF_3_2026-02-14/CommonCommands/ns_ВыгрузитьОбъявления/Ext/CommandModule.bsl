
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ОчиститьСообщения();
	НомерОтчетаСОшибками = ВыгрузитьОбъявленияНаСервере();
	Если НЕ НомерОтчетаСОшибками = Неопределено Тогда
		ns_АвитоОбработкаОшибокКлиент.ВывестиОтчетСОшибками(НомерОтчетаСОшибками);
	КонецЕсли; 
КонецПроцедуры

&НаСервере
Функция ВыгрузитьОбъявленияНаСервере()
	Возврат ns_АвитоВыгрузкаСервер.ВыгрузитьОбъявленияВXML();
КонецФункции