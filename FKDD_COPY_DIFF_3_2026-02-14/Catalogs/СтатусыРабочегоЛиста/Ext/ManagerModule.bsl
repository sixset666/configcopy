// процедура заполнения предопределенных элементов справочника
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	
	//Заполнение своства Отказ
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Отказ.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Истина;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 0;
	Иначе
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования		= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Истина;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		// ++ Alexku 29.08.2015
		//Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
		//	ТекущееСвойство.ПорядокВОтчете							= 0;
		//КонецЕсли;
		// -- Alexku 
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;
	
	//Заполнение своства Создан
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Создан.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Истина;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 1;
	Иначе
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Истина;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 1;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;
	
	//Заполнение своства Интерес
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Интерес.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Ложь;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 2;
	Иначе
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Истина;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Ложь;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Ложь;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 2;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;	
	
	//Заполнение своства Продажа
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Продажа.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Ложь;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 3;
	Иначе
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Истина;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Ложь;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Ложь;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 3;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;
	
	//Заполнение своства Сделка
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Сделка.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Ложь;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 4;
	Иначе
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Истина;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Ложь;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 4;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;	
	
	//Заполнение своства Подготовка
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Подготовка.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Истина;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Истина;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 5;
	Иначе
		Если ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Ложь;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Истина;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Истина;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 5;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;
	
	//Заполнение своства Рассмотрение
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Рассмотрение.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Истина;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 6;
	Иначе
		Если ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Ложь;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Ложь;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Истина;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 6;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;
	
	//Заполнение своства Одобрен
	ТекущееСвойство = Справочники.СтатусыРабочегоЛиста.Одобрен.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТекущееСвойство.ИспользоватьВРабочемЛисте					= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования	= Ложь;
		ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела	= Истина;
		ТекущееСвойство.ВероятностьПродажи							= 50;
		ТекущееСвойство.ПорядокВОтчете								= 7;
	Иначе
		Если ТекущееСвойство.ИспользоватьВРабочемЛисте Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛисте			= Ложь;
		КонецЕсли;
		Если ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеОтделаСтрахования= Ложь;
		КонецЕсли;
		Если НЕ ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела Тогда
			ТекущееСвойство.ИспользоватьВРабочемЛистеКредитногоОтдела= Истина;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ВероятностьПродажи) Тогда
			ТекущееСвойство.ВероятностьПродажи						= 50;
		КонецЕсли;		
		Если обЗначениеНеЗаполнено(ТекущееСвойство.ПорядокВОтчете) Тогда
			ТекущееСвойство.ПорядокВОтчете							= 7;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ТекущееСвойство.ЭтоНовый();
	Если ТекущееСвойство.Модифицированность() Тогда
		Попытка
			ТекущееСвойство.Записать();
			#Если Клиент Тогда
			Сообщить("Справочник ""Статусы рабочего листа"". "+?(НовыйЭлемент,"Создан","Обновлен")+" элемент """+СокрЛП(ТекущееСвойство.Наименование)+"""");
			#КонецЕсли
		Исключение КонецПопытки;
	КонецЕсли;	
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
