// Процедура заполнения предопределенных элементов справочника
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	КарточкиДисконтнаяКарта = Справочники.Карточки.ДисконтнаяКарта.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиДисконтнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.ДисконтнаяКарта;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиДисконтнаяКарта.ВидКарточки) Тогда
		КарточкиДисконтнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.ДисконтнаяКарта;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиДисконтнаяКарта.ЭтоНовый();
	Если КарточкиДисконтнаяКарта.Модифицированность() Тогда
		Попытка
			КарточкиДисконтнаяКарта.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиДисконтнаяКарта.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиНеНайденнаяКарта = Справочники.Карточки.НеНайденнаяКарта.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиНеНайденнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.ДисконтнаяКарта;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиНеНайденнаяКарта.ВидКарточки) Тогда
		КарточкиНеНайденнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.ДисконтнаяКарта;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиНеНайденнаяКарта.ЭтоНовый();
	Если КарточкиНеНайденнаяКарта.Модифицированность() Тогда
		Попытка
			КарточкиНеНайденнаяКарта.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиНеНайденнаяКарта.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиКарточкаПользователя = Справочники.Карточки.КарточкаПользователя.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиКарточкаПользователя.ВидКарточки = Перечисления.ВидыКарточек.КарточкаПользователя;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиКарточкаПользователя.ВидКарточки) Тогда
		КарточкиКарточкаПользователя.ВидКарточки = Перечисления.ВидыКарточек.КарточкаПользователя;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиКарточкаПользователя.ЭтоНовый();
	Если КарточкиКарточкаПользователя.Модифицированность() Тогда
		Попытка
			КарточкиКарточкаПользователя.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиКарточкаПользователя.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиСлужебнаяОперация = Справочники.Карточки.СлужебнаяОперация.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиСлужебнаяОперация.ВидКарточки = Перечисления.ВидыКарточек.СлужебнаяОперация;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиСлужебнаяОперация.ВидКарточки) Тогда
		КарточкиСлужебнаяОперация.ВидКарточки = Перечисления.ВидыКарточек.СлужебнаяОперация;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиСлужебнаяОперация.ЭтоНовый();
	Если КарточкиСлужебнаяОперация.Модифицированность() Тогда
		Попытка
			КарточкиСлужебнаяОперация.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиСлужебнаяОперация.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиЧистая = Справочники.Карточки.Чистая.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиЧистая.ВидКарточки = Перечисления.ВидыКарточек.Чистая;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиЧистая.ВидКарточки) Тогда
		КарточкиЧистая.ВидКарточки = Перечисления.ВидыКарточек.Чистая;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиЧистая.ЭтоНовый();
	Если КарточкиЧистая.Модифицированность() Тогда
		Попытка
			КарточкиЧистая.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиЧистая.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиКлубнаяКарта = Справочники.Карточки.КлубнаяКарта.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиКлубнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.КлубнаяКарта;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиКлубнаяКарта.ВидКарточки) Тогда
		КарточкиКлубнаяКарта.ВидКарточки = Перечисления.ВидыКарточек.КлубнаяКарта;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиКлубнаяКарта.ЭтоНовый();
	Если КарточкиКлубнаяКарта.Модифицированность() Тогда
		Попытка
			КарточкиКлубнаяКарта.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиКлубнаяКарта.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	КарточкиТалон = Справочники.Карточки.Талон.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		КарточкиТалон.ВидКарточки = Перечисления.ВидыКарточек.Талон;
	ИначеЕсли обЗначениеНеЗаполнено(КарточкиТалон.ВидКарточки) Тогда
		КарточкиТалон.ВидКарточки = Перечисления.ВидыКарточек.Талон;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = КарточкиТалон.ЭтоНовый();
	Если КарточкиТалон.Модифицированность() Тогда
		Попытка
			КарточкиТалон.Записать();
			Сообщить("Справочник ""Карточки"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(КарточкиТалон.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;	
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
