// Заполняет предопределенные элементы(если они не существуют - создает новые(не предопределенные!))
// при обновлении релиза
// Флаг ЗаполнятьВсе=Истина - перезаполняет уже существующие элементы(строгое заполнение)
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	Запрос=Новый Запрос("ВЫБРАТЬ
	                    |	КатегорииТранспортныхСредств.Ссылка КАК Ссылка,
	                    |	КатегорииТранспортныхСредств.Наименование КАК Наименование
	                    |ИЗ
	                    |	Справочник.КатегорииТранспортныхСредств КАК КатегорииТранспортныхСредств
	                    |ГДЕ
	                    |	КатегорииТранспортныхСредств.Предопределенный = ИСТИНА
	                    |	И КатегорииТранспортныхСредств.ЭтоГруппа = ЛОЖЬ
	                    |	"+?(ЗаполнятьВсе,"","И КатегорииТранспортныхСредств.КодПоКлассификатору = """"")+"
	                    |
	                    |УПОРЯДОЧИТЬ ПО
	                    |	Ссылка");
	ВыборкаКатегорий=Запрос.Выполнить().Выбрать();
	Пока ВыборкаКатегорий.Следующий() Цикл
		КатегорияСсылка=ВыборкаКатегорий.Ссылка;
		Пробел=Найти(КатегорияСсылка.Наименование," ");
		Если Пробел>0 Тогда
			КодПоКлассификатору=Лев(КатегорияСсылка.Наименование,Пробел-1);
			КатегорияОбъект=КатегорияСсылка.ПолучитьОбъект();
			КатегорияОбъект.КодПоКлассификатору=КодПоКлассификатору;
			КатегорияОбъект.ОбменДанными.Загрузка=Истина;
			Попытка
				КатегорияОбъект.Записать();
				Сообщить("Справочник ""Категории транспортных средств"". Обновлен элемент """+СокрЛП(КатегорияСсылка.Наименование)+"""");
			Исключение
			КонецПопытки; 
		КонецЕсли; 
	КонецЦикла;
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
