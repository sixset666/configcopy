// Процедура заполнения преопределенных элементов
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	// Наличные
	ТипыОплатНаличные = Справочники.ТипыОплат.Наличные.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатНаличные.ВидОплаты = Перечисления.ВидыОплаты.НаличныйРасчет;
		ТипыОплатНаличные.Объект = Перечисления.ТипыОплатыВРознице.Наличные;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатНаличные.ВидОплаты) Тогда
			ТипыОплатНаличные.ВидОплаты = Перечисления.ВидыОплаты.НаличныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатНаличные.Объект) Тогда
			ТипыОплатНаличные.Объект = Перечисления.ТипыОплатыВРознице.Наличные;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатНаличные.ЭтоНовый();
	Если ТипыОплатНаличные.Модифицированность() Тогда
		Попытка
			ТипыОплатНаличные.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатНаличные.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// БезНаличные
	ТипыОплатБезНаличные = Справочники.ТипыОплат.Безналичные.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатБезНаличные.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		ТипыОплатБезНаличные.Объект = Перечисления.ТипыОплатыВРознице.Безналичные;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатБезНаличные.ВидОплаты) Тогда
			ТипыОплатБезНаличные.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатБезНаличные.Объект) Тогда
			ТипыОплатБезНаличные.Объект = Перечисления.ТипыОплатыВРознице.Безналичные;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатБезНаличные.ЭтоНовый();
	Если ТипыОплатБезНаличные.Модифицированность() Тогда
		Попытка
			ТипыОплатБезНаличные.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатБезНаличные.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// Кредит
	ТипыОплатКредит = Справочники.ТипыОплат.Кредит.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатКредит.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		ТипыОплатКредит.Объект = Перечисления.ТипыОплатыВРознице.Кредит;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатКредит.ВидОплаты) Тогда
			ТипыОплатКредит.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатКредит.Объект) Тогда
			ТипыОплатКредит.Объект = Перечисления.ТипыОплатыВРознице.Кредит;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатКредит.ЭтоНовый();
	Если ТипыОплатКредит.Модифицированность() Тогда
		Попытка
			ТипыОплатКредит.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатКредит.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// Талон
	ТипыОплатТалон = Справочники.ТипыОплат.Талон.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатТалон.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		ТипыОплатТалон.Объект = Перечисления.ТипыОплатыВРознице.Талон;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатТалон.ВидОплаты) Тогда
			ТипыОплатТалон.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатТалон.Объект) Тогда
			ТипыОплатТалон.Объект = Перечисления.ТипыОплатыВРознице.Талон;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатТалон.ЭтоНовый();
	Если ТипыОплатТалон.Модифицированность() Тогда
		Попытка
			ТипыОплатТалон.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатТалон.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// Клубная карта
	ТипыОплатКлубнаяКарта = Справочники.ТипыОплат.КлубнаяКарта.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатКлубнаяКарта.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		ТипыОплатКлубнаяКарта.Объект = Перечисления.ТипыОплатыВРознице.КлубнаяКарта;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатКлубнаяКарта.ВидОплаты) Тогда
			ТипыОплатКлубнаяКарта.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатКлубнаяКарта.Объект) Тогда
			ТипыОплатКлубнаяКарта.Объект = Перечисления.ТипыОплатыВРознице.КлубнаяКарта;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатКлубнаяКарта.ЭтоНовый();
	Если ТипыОплатКлубнаяКарта.Модифицированность() Тогда
		Попытка
			ТипыОплатКлубнаяКарта.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатКлубнаяКарта.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// ЗаСчетЗаведения
	ТипыОплатЗаСчетЗаведения = Справочники.ТипыОплат.ЗаСчетЗаведения.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатЗаСчетЗаведения.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		ТипыОплатЗаСчетЗаведения.Объект = Перечисления.ТипыОплатыВРознице.ЗаСчетЗаведения;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатЗаСчетЗаведения.ВидОплаты) Тогда
			ТипыОплатЗаСчетЗаведения.ВидОплаты = Перечисления.ВидыОплаты.БезналичныйРасчет;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатЗаСчетЗаведения.Объект) Тогда
			ТипыОплатЗаСчетЗаведения.Объект = Перечисления.ТипыОплатыВРознице.ЗаСчетЗаведения;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатЗаСчетЗаведения.ЭтоНовый();
	Если ТипыОплатЗаСчетЗаведения.Модифицированность() Тогда
		Попытка
			ТипыОплатЗаСчетЗаведения.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатЗаСчетЗаведения.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// ПостоплатаКредит
	ТипыОплатПостоплатаКредит = Справочники.ТипыОплат.ПостоплатаКредит.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ТипыОплатПостоплатаКредит.ВидОплаты = Перечисления.ВидыОплаты.ПроизвольнаяОплата;
		ТипыОплатПостоплатаКредит.Объект = Перечисления.ТипыОплатыВРознице.ЗаСчетЗаведения;
		ТипыОплатПостоплатаКредит.ТипОплатыККТ = Перечисления.ТипыОплатыККТ.Постоплата;
	Иначе
		Если обЗначениеНеЗаполнено(ТипыОплатПостоплатаКредит.ВидОплаты) Тогда
			ТипыОплатПостоплатаКредит.ВидОплаты = Перечисления.ВидыОплаты.ПроизвольнаяОплата;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатПостоплатаКредит.Объект) Тогда
			ТипыОплатПостоплатаКредит.Объект = Перечисления.ТипыОплатыВРознице.ЗаСчетЗаведения;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ТипыОплатПостоплатаКредит.ТипОплатыККТ) Тогда
			ТипыОплатПостоплатаКредит.ТипОплатыККТ = Перечисления.ТипыОплатыККТ.Постоплата;
		КонецЕсли;
	КонецЕсли;
	//Сохранение справочника
	НовыйЭлемент = ТипыОплатПостоплатаКредит.ЭтоНовый();
	Если ТипыОплатПостоплатаКредит.Модифицированность() Тогда
		Попытка
			ТипыОплатПостоплатаКредит.Записать();
			Сообщить("Справочник ""ТипыОплат"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ТипыОплатПостоплатаКредит.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
