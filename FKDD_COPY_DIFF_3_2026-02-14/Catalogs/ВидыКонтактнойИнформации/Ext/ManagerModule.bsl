// процедура заполнения предопределенной контактной информации
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	//Заполним типы предопределенной контактной информации
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресИностранный.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресПочтовый.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресФактический.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресЮридический.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресЭлектроннойПочтыДомашний.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.АдресЭлектроннойПочтыРабочий.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ВебСтраницаОрганизации.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.ВебСтраница;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ВебСтраницаФизЛица.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.ВебСтраница;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.НомерICQ.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.НомерICQ;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ТелефонДомашний.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ТелефонРабочий.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ТелефонСотовый.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.Факс.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ТелефонКонтактный.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
		ОбъектВид.Записать();
	КонецЕсли;
	ОбъектВид = Справочники.ВидыКонтактнойИнформации.ПрочаяИнформация.ПолучитьОбъект();
	Если обЗначениеНеЗаполнено(ОбъектВид.Тип) Тогда
		ОбъектВид.Тип = Перечисления.ТипыКонтактнойИнформации.Другое;
		ОбъектВид.Записать();
	КонецЕсли;
    	
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
