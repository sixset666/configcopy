
&НаКлиенте
Процедура КомандаОк(Команда)
	
	Закрыть(СписокЗначений);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//ЭтаФорма.СписокЗначений.ЗагрузитьЗначения(Параметры.СписокВыбора.ВыгрузитьЗначения());
	ЭтаФорма.СписокЗначений                   = Параметры.СписокВыбора;
	ЭтаФорма.СписокЗначений.ТипЗначения       = Параметры.ТипЗначения;
	ЭтаФорма.СписокЗначений.ДоступныеЗначения = Параметры.ДоступныеЗначения;
	ТолькоГруппы                              = ТолькоГруппы;
	Если Параметры.Свойство("ПараметрОтборПоВладельцу") Тогда
		ПараметрОтборПоВладельцу = Параметры.ПараметрОтборПоВладельцу;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СписокЗначенийЗначениеНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если ТолькоГруппы Тогда
		Элемент.ВыборГруппИЭлементов = ГруппыИЭлементы.Группы;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрОтборПоВладельцу) Тогда
		НовыйПараметр = Новый ПараметрВыбора("Отбор.Владелец", ПараметрОтборПоВладельцу);
		НовыйМассив = Новый Массив();
		НовыйМассив.Добавить(НовыйПараметр);
		Элемент.ПараметрыВыбора = Новый ФиксированныйМассив(НовыйМассив);
	КонецЕсли;
	
КонецПроцедуры
