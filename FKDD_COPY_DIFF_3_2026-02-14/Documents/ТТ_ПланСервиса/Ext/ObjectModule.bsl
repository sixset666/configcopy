
Процедура ОбработкаПроведения(Отказ, Режим)

	// регистр ТТ_ПланСервиса 
	Движения.ТТ_ПланСервиса.Записывать = Истина;
	Для Каждого ТекСтрокаПлан Из План Цикл
		Движение = Движения.ТТ_ПланСервиса.Добавить();
		Движение.Период = Дата;
		Движение.Организация = ТекСтрокаПлан.Организация;
		Движение.Показатель = ТекСтрокаПлан.ПоказательСервиса;
		Движение.Бренд = ТекСтрокаПлан.Бренд;
		Движение.Сумма = ТекСтрокаПлан.Сумма;
	КонецЦикла;


КонецПроцедуры
