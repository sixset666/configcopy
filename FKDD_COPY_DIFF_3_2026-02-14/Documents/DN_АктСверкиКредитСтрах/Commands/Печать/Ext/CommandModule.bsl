
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Для Каждого Документ из ПараметрКоманды Цикл
		Если НЕ Документ.Проведен тогда
			Если ПараметрКоманды.Количество() = 1 тогда
				Предупреждение("Документ не проведен, печать возможна только после проведения",,"Ошибка печати");
				Возврат;
			Иначе
				Предупреждение("Не все выбранные документы проведены, печать возможна только после проведения",,"Ошибка печати");
				Возврат;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	ТабДок = Новый ТабличныйДокумент;
	Печать(ТабДок, ПараметрКоманды);

	ТабДок.ОтображатьСетку = Ложь;
	ТабДок.Защита = Ложь;
	ТабДок.ТолькоПросмотр = Истина;
	ТабДок.ОтображатьЗаголовки = Ложь;
	ТабДок.Показать();
	
КонецПроцедуры

&НаСервере
Процедура Печать(ТабДок, ПараметрКоманды)
	Документы.DN_АктСверкиКредитСтрах.Печать(ТабДок, ПараметрКоманды);
КонецПроцедуры
