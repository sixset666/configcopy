
Процедура ПередВыполнением(Отказ)
	
	Если НЕ ЗначениеЗаполнено(Пользователь) Тогда
		Пользователь	= ПараметрыСеанса.Пользователь;
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ДатаВыполнения) Тогда
		ДатаВыполнения	= ТекущаяДата();
	КонецЕсли; 
	
	Выполнена = Истина;
	
КонецПроцедуры

Процедура ПриВыполнении(Отказ)
	БТ_БизнесПроцессы.ДобавитьСогласующегоВЗаписьСтатусовЗаявок(БизнесПроцесс, Пользователь, , Роль, ДатаВыполнения, ТочкаМаршрута);
КонецПроцедуры

