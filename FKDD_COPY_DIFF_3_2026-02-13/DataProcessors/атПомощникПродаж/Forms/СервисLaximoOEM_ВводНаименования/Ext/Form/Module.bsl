
&НаКлиенте
Процедура Выбрать()
	ЭтаФорма.Закрыть(Новый Структура("ИмяНабора, Наименование", ИмяНабора, Наименование));
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ИмяНабора = Параметры.ИмяНабора;
	Наименование = Параметры.Наименование;
КонецПроцедуры

