Процедура ЗарегистрироватьОценкуЛояльностиКлиента()  Экспорт
	
	ДатаОценки = ТекущаяДата();
	НаборЗаписейОЛК = РегистрыСведений.ОценкаЛояльностиКлиентов.СоздатьНаборЗаписей();
	
	НаборЗаписейОЛК.Отбор.Контрагент.Установить(Контрагент);
	НаборЗаписейОЛК.Отбор.Документ.Установить(Документ);
	НаборЗаписейОЛК.Отбор.Период.Установить(ДатаОценки);
	
	НаборЗаписейОЛК.Прочитать();
	
	Запись = НаборЗаписейОЛК.Добавить();
	Запись.Контрагент = Контрагент;
	Запись.Документ = Документ;
	Запись.Период = ДатаОценки;
	Запись.Оценка = Оценка;
	Запись.Обоснование = Обоснование;
	Запись.Автор = Автор;
	
	НаборЗаписейОЛК.Записать();
	
	
КонецПроцедуры