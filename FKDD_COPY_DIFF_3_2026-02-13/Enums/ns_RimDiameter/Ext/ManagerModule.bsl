Функция ПолучитьЗначениеПеречисления(ЗначениеРеквизита) Экспорт
	ЗначениеПеречисления = Неопределено;
	
	Попытка
		ЗначениеСтр = СтрЗаменить(Строка(ЗначениеРеквизита), ".", "_");
		ЗначениеСтр = "_" + ЗначениеСтр;
	
		ЗначениеПеречисления = Перечисления.ns_RimDiameter[ЗначениеСтр];
	Исключение
	КонецПопытки; 
	
	Возврат ЗначениеПеречисления;
КонецФункции
 