// процедура заполнения предопределенных элементов справочника
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	ЦехаОсновнойЦех = Справочники.Цеха.ОсновнойЦех.ПолучитьОбъект();
	Если ЗаполнятьВсе Тогда
		ЦехаОсновнойЦех.Код="00000";
		ЦехаОсновнойЦех.Наименование = "Основной цех";
		ЦехаОсновнойЦех.Подразделение = Справочники.ПодразделенияКомпании.ОсновноеПодразделение;
		ЦехаОсновнойЦех.Организация = Справочники.Организации.ОсновнаяОрганизация;
		ЦехаОсновнойЦех.СкладКомпании = Справочники.СкладыКомпании.ОсновнойСкладКомпании;
		ЦехаОсновнойЦех.ГрафикРаботы = ЦехаОсновнойЦех.Подразделение.ГрафикРаботы;
		Если НЕ обЗначениеНеЗаполнено(ЦехаОсновнойЦех.Подразделение.ГрафикРаботы) Тогда
			ЦехаОсновнойЦех.ГрафикРаботы = ЦехаОсновнойЦех.Подразделение.ГрафикРаботы;
		КонецЕсли;
	Иначе
		Если обЗначениеНеЗаполнено(ЦехаОсновнойЦех.Наименование) Тогда
			ЦехаОсновнойЦех.Наименование = "Основной цех";
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ЦехаОсновнойЦех.Подразделение) Тогда
			ЦехаОсновнойЦех.Подразделение = Справочники.ПодразделенияКомпании.ОсновноеПодразделение;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ЦехаОсновнойЦех.Организация) Тогда
			ЦехаОсновнойЦех.Организация = Справочники.Организации.ОсновнаяОрганизация;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ЦехаОсновнойЦех.СкладКомпании) Тогда
			ЦехаОсновнойЦех.СкладКомпании = Справочники.СкладыКомпании.ОсновнойСкладКомпании;
		КонецЕсли;
		Если обЗначениеНеЗаполнено(ЦехаОсновнойЦех.ГрафикРаботы) Тогда
			Если НЕ обЗначениеНеЗаполнено(ЦехаОсновнойЦех.Подразделение.ГрафикРаботы) Тогда
				ЦехаОсновнойЦех.ГрафикРаботы = ЦехаОсновнойЦех.Подразделение.ГрафикРаботы;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	НовыйЭлемент = ЦехаОсновнойЦех.ЭтоНовый();
	Если ЦехаОсновнойЦех.Модифицированность() Тогда
		Попытка
			ЦехаОсновнойЦех.Записать();
			Сообщить("Справочник ""Цеха"". "+?(НовыйЭлемент,"Добавлен","Обновлен")+" элемент """+СокрЛП(ЦехаОсновнойЦех.Наименование)+"""");
		Исключение
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
