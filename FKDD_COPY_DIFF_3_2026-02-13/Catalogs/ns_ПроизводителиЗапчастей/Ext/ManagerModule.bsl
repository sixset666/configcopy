Функция НайтиЭлементПоНаименованию(Наименование) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ns_ПроизводителиЗапчастей.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ns_ПроизводителиЗапчастей КАК ns_ПроизводителиЗапчастей
	|ГДЕ
	|	ns_ПроизводителиЗапчастей.Наименование = &Наименование";
	Запрос.УстановитьПараметр("Наименование", Наименование);
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Неопределено;
	КонецЕсли; 	
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	
	Возврат Выборка.Ссылка;
КонецФункции
 