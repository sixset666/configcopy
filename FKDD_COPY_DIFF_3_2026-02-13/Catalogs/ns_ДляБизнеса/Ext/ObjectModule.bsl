Процедура ПередЗаписью(Отказ)
	Если НЕ ЭтоГруппа Тогда
		Title = ns_ШаблонизаторВызовСервера.ОчиститьXML(Title);
	КонецЕсли;
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	ОбязательныеПоля = ns_ДляБизнесаВалидация.ПолучитьОбязательныеПоля();
	Для каждого ИмяПоля Из ОбязательныеПоля Цикл
		ПроверяемыеРеквизиты.Добавить(ИмяПоля);
	КонецЦикла; 
	
	ДополнительныеОбязательныеПоля = ns_ДляБизнесаВалидация.ПолучитьДополнительныеОбязательныеПоля(Category);
	Для каждого ИмяПоля Из ДополнительныеОбязательныеПоля Цикл
		ПроверяемыеРеквизиты.Добавить(ИмяПоля);
	КонецЦикла; 
	
КонецПроцедуры
