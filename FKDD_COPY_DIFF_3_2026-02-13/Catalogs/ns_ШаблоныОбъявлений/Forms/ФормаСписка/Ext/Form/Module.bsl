&НаКлиенте
Процедура ВыгрузитьДанные(Команда)
	
	АдресВХранилище = ВыгрузитьДанныеНаСервере(УникальныйИдентификатор);
	
	//Если НужноФормироватьОтчет(АдресВХранилище) Тогда
	//	ПараметрыФормы = Новый Структура;
	//	ПараметрыФормы.Вставить("АдресВХранилище", АдресВХранилище);
	//	ПараметрыФормы.Вставить("СформироватьПриОткрытии", Истина);
	//	ФормаОтчета = ПолучитьФорму("Отчет.ОтчетОшибкиАвито.Форма.ФормаОтчетаУпр", ПараметрыФормы);
	//	ФормаОтчета.Открыть();
	//КонецЕсли; 
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ВыгрузитьДанныеНаСервере(УникальныйИдентификатор)
	Возврат ns_АвитоВыгрузкаСервер.ВыгрузитьОбъявленияВXML(УникальныйИдентификатор); 	
КонецФункции
  
