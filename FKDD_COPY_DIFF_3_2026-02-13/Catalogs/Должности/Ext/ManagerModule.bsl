// процедура заполнения предопределенных элементов
//
// Параметры:
// ЗаполнятьВсе - Булево - Признак заполнение всей информации
//
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	
	//Заполним предопределенными работами 
	Объект = Справочники.Должности.Диспетчер.ПолучитьОбъект();
	Если Объект.Работы.Количество()=0 Тогда
		НоваяСтрока = Объект.Работы.Добавить();
		НоваяСтрока.Авторабота = Справочники.Автоработы.Приемка;
		НоваяСтрока = Объект.Работы.Добавить();
		НоваяСтрока.Авторабота = Справочники.Автоработы.Выдача;
		Попытка
			Объект.Записать();
		Исключение
			Сообщить("Ошибка обновления должности <"+Объект+">: "+ОписаниеОшибки());
		КонецПопытки; 
	КонецЕсли;
	
КонецПроцедуры // ЗаполнитьПредопределенныеЭлементы()
