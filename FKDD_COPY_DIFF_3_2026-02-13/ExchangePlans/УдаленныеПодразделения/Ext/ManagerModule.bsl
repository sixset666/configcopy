// Заполняет предопределенный узел (этот узел)
// 
Процедура ЗаполнитьПредопределенныеЭлементы(ЗаполнятьВсе=Ложь) Экспорт
	
	ГлавныйУзелИБ = ПланыОбмена.ГлавныйУзел();
	Если (ГлавныйУзелИБ = Неопределено) ИЛИ (ГлавныйУзелИБ = ЭтотУзел()) Тогда
		ГлавныйУзелИБ = ЭтотУзел().ПолучитьОбъект();
		Если ПустаяСтрока(ГлавныйУзелИБ.Код) Тогда ГлавныйУзелИБ.Код = "ЦБ"; КонецЕсли;
		Если ПустаяСтрока(ГлавныйУзелИБ.Наименование) Тогда ГлавныйУзелИБ.Наименование = "Центральная база"; КонецЕсли;
		
		Если ГлавныйУзелИБ.Модифицированность() Тогда
			Попытка	ГлавныйУзелИБ.Записать();
			Исключение КонецПопытки;
		КонецЕсли;
	КонецЕсли; 	
КонецПроцедуры
