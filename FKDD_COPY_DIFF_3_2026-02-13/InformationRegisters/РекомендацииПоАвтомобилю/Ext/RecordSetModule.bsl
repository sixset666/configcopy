
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если ОбЗначениеНеЗаполнено(Запись.Рекомендация) И ОбЗначениеНеЗаполнено(Запись.Комментарий) Тогда
			Отказ = Истина;
			СтрокаОтказа = "Не заполнены поля ""Рекомендация"" и ""Комментарий"". Запись невозможна!";
			Продолжить;
		КонецЕсли; 
	КонецЦикла;
	
	Если Отказ Тогда
		Сообщить(СтрокаОтказа);
	КонецЕсли; 
	
КонецПроцедуры
