// запись набора записи
//
Процедура ПриЗаписи(Отказ, Замещение)
	
	Если обЭтотТип(Отбор.Объект.Значение, "СправочникСсылка.Номенклатура") Тогда
		ЕстьНеВнешниеФайлы = Ложь;
		
		// заригестрируем изменения номенклатуры
		Для Каждого Запись Из ЭтотОбъект Цикл
			//ЕстьНеВнешниеФайлы = Ложь;
			
			Если Запись.ДанныеВоВнешнемФайле ИЛИ НЕ Запись.Картинка Тогда
				Продолжить;
			КонецЕсли;
			
			ЕстьНеВнешниеФайлы = Истина; Прервать;
		КонецЦикла;
		
		Если ЕстьНеВнешниеФайлы Тогда 
			Выборка = ПланыОбмена.ОбменССайтом.Выбрать();
			Узлы = Новый Массив;
			Пока Выборка.Следующий() Цикл
				Если Выборка.Ссылка = ПланыОбмена.ОбменССайтом.ЭтотУзел() Тогда
					Продолжить;
				КонецЕсли;
				
				Узлы.Добавить(Выборка.Ссылка);
			КонецЦикла;
		
			ПланыОбмена.ЗарегистрироватьИзменения(Узлы, Отбор.Объект.Значение);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
